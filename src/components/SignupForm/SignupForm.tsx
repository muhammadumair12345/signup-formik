import React, { FC } from "react";
import { Form } from "./SignupForm.style";

const SignupForm: FC = () => {
  return (
    <Form>
      <input type="number" />
    </Form>
  );
};

export default SignupForm;
